name: run-tests
on: [push]
jobs:
    tests:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - name: Test with pytest
          run: |
                pip install pytest
                pytest
