name: run-tests
on: [push]
jobs:
    tests:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - run: |
                pip install pytest
                python -m pytest tests/power-tests.py
